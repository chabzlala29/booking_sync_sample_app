.row
  - @rentals.in_groups_of(3) do |group|
    - group.compact.each do |rental|
      .col-md-4
        %h2= rental.name
        %p= rental.summary.en
        %p
          = link_to 'View details', rental, class: 'btn btn-default'
